# Used by Django via django-environ in config/settings.py
# For Docker, compose also sets DATABASE_URL directly; this file is helpful for host-based development.
DATABASE_URL=postgres://attendance_user:test123@localhost:5433/attendance_db
